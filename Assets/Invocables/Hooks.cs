/*
 * Used to identify event hooks as well as initialising them.
 */

using System;
using UnityEngine;

namespace Invocables {
	public enum HOOKS {PLAYER_INIT = 1, PLAYER_INIT_SCENE, USED_ITEM, THREW_POKEBALL, SELECTED_PARTY_POKEMON};
}
